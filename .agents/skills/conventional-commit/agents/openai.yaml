interface:
  display_name: "Conventional Commit"
  short_description: "Draft and create Conventional Commits"
  default_prompt: "Use $conventional-commit to inspect the current git changes and create a Conventional Commits v1.0.0 commit."
