# Project-scoped Codex configuration for subagent workflows.
# Keep this file conservative. Runtime permissions still depend on the user's Codex session settings.

project_doc_fallback_filenames = [".agents.md"]
project_doc_max_bytes = 65536

[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800
